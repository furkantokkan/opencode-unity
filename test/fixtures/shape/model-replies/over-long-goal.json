{"goal":"Fix the broken inventory so that every slot, every item, every stack count, every icon, every tooltip, every drag handle, every hover state, every selection outline, every equip marker, every durability bar, every rarity frame, every lock overlay, every quantity badge, every sort order, every filter tab, every search box and every scroll position is refreshed only when the inventory model actually changes.","files":[],"search":"","done":"The inventory refreshes only on change.","open":[]}
